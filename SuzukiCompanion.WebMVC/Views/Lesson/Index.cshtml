@model IEnumerable<SuzukiCompanion.Models.LessonListItem>

@{
    ViewBag.Title = "Index";
}

<h2>Lessons</h2>

@if (User.Identity.IsAuthenticated)
{

    if (User.IsInRole("Admin"))
    {

        <div class="homepage-body">

               
                    @Html.ActionLink("Create New Lesson", "Create", new { area = "" }, new { @class = "btn btn-primary" })
           
        </div>
    }

}

@if (TempData.ContainsKey("SaveResult"))
{
    <div class="alert alert-success fade in" role="alert">

        <button type="button" class="close" data-dismiss="alert">
            <span>&times;</span>
        </button>
        @TempData["SaveResult"]
    </div>
}
<div class="homepage-body table-bordered">
    <p><u>List of Lessons </u> </p>
</div>
<table class="table homepage-body table-bordered">
    <tr>
        @*<th>
            @Html.DisplayNameFor(model => model.LessonName)
        </th>*@

    @*<th>
            @Html.DisplayNameFor(model => model.CreatedUtc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ModifiedUtc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Contents)
        </th>*@
    @*<th>List of Lessons</th>*@
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LessonName)
            </td>
            @*<td>
                    @Html.DisplayFor(modelItem => item.CreatedUtc)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ModifiedUtc)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Contents)
                </td>*@
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.LessonId }, new { @class = "btn btn-primary" })
                @Html.ActionLink("Detail", "Detail", new { id = item.LessonId }, new { @class = "btn btn-primary" })
                @Html.ActionLink("Delete", "Delete", new { id = item.LessonId }, new { @class = "btn btn-primary" })
            </td>
        </tr>
    }
</table>
